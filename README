1. create virual environment
2. add all dependencies by running "pip install -r requirements.txt" in the terminal
3. Only run "colab_filtering_main.py" as is and wait, it will take hours, adjust as wanted to do less clusters